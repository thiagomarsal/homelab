---
# Managed by Ansible — see ansible/roles for secret provisioning
# Secret value should be stored in ansible/inventory/group_vars/all.yml (vault encrypted)
# Do NOT apply this file manually or commit real credential values.
#
# To create manually (pre-deploy):
#   kubectl create secret generic jenkins-admin-secret \
#     --from-literal=jenkins-admin-user=admin \
#     --from-literal=jenkins-admin-password=<password> \
#     -n jenkins
