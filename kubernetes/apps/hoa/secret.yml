---
# Managed by Ansible — see ansible/roles/rancher/templates/hoa-secret.yml.j2
# Secret values stored in ansible/inventory/group_vars/all.yml (vault encrypted:
# hoa_db_password, hoa_db_root_password).
# Do NOT apply this file manually or commit real credential values.
