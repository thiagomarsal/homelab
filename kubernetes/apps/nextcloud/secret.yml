---
# Real values applied out-of-band, never committed:
#   kubectl create secret generic nextcloud-db-secret -n nextcloud \
#     --from-literal=MYSQL_ROOT_PASSWORD='...' \
#     --from-literal=MYSQL_PASSWORD='...' \
#     --from-literal=MYSQL_USER=ncadmin \
#     --from-literal=MYSQL_DATABASE=nextcloud
#   kubectl create secret generic nextcloud-redis-secret -n nextcloud \
#     --from-literal=REDIS_HOST_PASSWORD='...'
# Do NOT apply this file manually or commit real credential values.
