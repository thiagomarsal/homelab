apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  namespace: minio
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: minio-pv
  resources:
    requests:
      storage: 100Gi
